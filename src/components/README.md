Reusable UI components: ChatMessage, ChatInput, VoiceRecorder, VoicePlayer, ThinkingIndicator,
SourceCitation, InsightCard, ActionPlan, SessionList, DocumentUploader. Every component with real
visual surface goes through the Impeccable workflow — see `docs/UI_DESIGN.md`.
