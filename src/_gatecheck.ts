// TEMPORARY — deliberately breaks `npm run typecheck` to prove the CI deploy gate.
// Revert (git revert) once you've confirmed the `deploy` job is Skipped on the failed run.
export const broken: number = "nope";
