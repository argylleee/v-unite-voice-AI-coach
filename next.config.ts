import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev badge (dev only; no effect on production).
  devIndicators: false,
};

export default nextConfig;
